# Migration `20200115103805-biograhpy-optional`

This migration has been generated by Vincent Poulain at 1/15/2020, 10:38:05 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."User" ADD COLUMN "updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
DROP COLUMN "biography",
ADD COLUMN "biography" text   ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20200106193010-first..20200115103805-biograhpy-optional
--- datamodel.dml
+++ datamodel.dml
@@ -3,19 +3,20 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model User {
   id           String       @default(cuid()) @id
   authUserId   String?      @unique
   slug         String       @unique
   createdAt    DateTime     @default(now())
+  updatedAt    DateTime     @updatedAt
   firstname    String
   pictureUrl   String
-  biography    String
+  biography    String?
   linkedin     String?
   github       String?
   mail         String?
   youtube      String?
```

## Photon Usage

You can use a specific Photon built for this migration (20200115103805-biograhpy-optional)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20200115103805-biograhpy-optional'

const photon = new Photon()

async function main() {
    const result = await photon.users()
    console.dir(result, { depth: null })
}

main()
```

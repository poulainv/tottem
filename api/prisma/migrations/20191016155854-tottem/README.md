# Migration `20191016155854-tottem`

This migration has been generated by Vincent Poulain at 10/16/2019, 3:58:54 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
  "biography" text NOT NULL DEFAULT '' ,
  "firstname" text NOT NULL DEFAULT '' ,
  "id" text NOT NULL  ,
  "label" text   ,
  "slug" text NOT NULL DEFAULT '' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "public"."Social" (
  "github" text   ,
  "id" text NOT NULL  ,
  "linkedin" text   ,
  "mail" text   ,
  "website" text   ,
  "youtube" text   ,
  PRIMARY KEY ("id")
);

CREATE TABLE "public"."Section" (
  "id" text NOT NULL  ,
  "index" integer NOT NULL DEFAULT 0 ,
  "name" text NOT NULL DEFAULT '' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "public"."Collection" (
  "date" text   ,
  "detail" text   ,
  "id" text NOT NULL  ,
  "name" text NOT NULL DEFAULT '' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "public"."Item" (
  "author" text NOT NULL DEFAULT '' ,
  "id" text NOT NULL  ,
  "imageUrl" text   ,
  "note" text   ,
  "productUrl" text NOT NULL DEFAULT '' ,
  "title" text NOT NULL DEFAULT '' ,
  "type" text NOT NULL DEFAULT 'Book' ,
  PRIMARY KEY ("id")
);

ALTER TABLE "public"."Social" ADD COLUMN "user" text   REFERENCES "public"."User"("id") ON DELETE SET NULL;

ALTER TABLE "public"."Section" ADD COLUMN "author" text   REFERENCES "public"."User"("id") ON DELETE SET NULL;

ALTER TABLE "public"."Collection" ADD COLUMN "author" text   REFERENCES "public"."User"("id") ON DELETE SET NULL,
ADD COLUMN "section" text   REFERENCES "public"."Section"("id") ON DELETE SET NULL;

ALTER TABLE "public"."Item" ADD COLUMN "collection" text   REFERENCES "public"."Collection"("id") ON DELETE SET NULL;

CREATE UNIQUE INDEX "User.slug" ON "public"."User"("slug")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191016155854-tottem
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,68 @@
+generator photon {
+  provider = "photonjs"
+}
+
+datasource db {
+  provider = env("DATABASE_PROVIDER")
+  url      = env("DATABASE_URL")
+}
+
+model User {
+  id        String    @default(cuid()) @id
+  slug      String    @unique
+  firstname String
+  biography String
+  social    Social?
+  label     String?
+  sections  Section[]
+
+}
+
+model Social {
+  id       String  @default(cuid()) @id
+  linkedin String?
+  github   String?
+  mail     String?
+  youtube  String?
+  website  String?
+}
+
+model Section {
+  id          String       @default(cuid()) @id
+  name        String
+  index       Int          @default(0)
+  collections Collection[]
+  author      User
+}
+
+model Collection {
+  id      String  @id @default(cuid())
+  name    String
+  detail  String?
+  items   Item[]
+  date    String?
+  author  User
+  section Section
+}
+
+model Item {
+  id         String     @default(cuid()) @id
+  title      String
+  author     String
+  type       ItemType
+  productUrl String
+  imageUrl   String?
+  note       String?
+  collection Collection
+}
+
+
+enum ItemType {
+  Book
+  Album
+  Movie
+  Video
+  People
+  Paper
+  Podcast
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191016155854-tottem)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191016155854-tottem'

const photon = new Photon()

async function main() {
    const result = await photon.users()
    console.dir(result, { depth: null })
}

main()
```

# Migration `20191016111406-first`

This migration has been generated by Vincent Poulain at 10/16/2019, 11:14:06 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."User" (
  "email" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "name" TEXT    ,
  PRIMARY KEY ("id")
);

CREATE TABLE "lift"."Post" (
  "author" TEXT    REFERENCES "User"(id) ON DELETE SET NULL,
  "content" TEXT    ,
  "id" TEXT NOT NULL   ,
  "published" BOOLEAN NOT NULL DEFAULT true  ,
  "title" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

CREATE TABLE "lift"."Category" (
  "id" TEXT NOT NULL   ,
  "name" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

CREATE TABLE "lift"."_CategoryToPost" (
  "A" TEXT    REFERENCES "Category"(id) ON DELETE CASCADE,
  "B" TEXT    REFERENCES "Post"(id) ON DELETE CASCADE
);

CREATE UNIQUE INDEX "lift"."User.email" ON "User"("email")

CREATE UNIQUE INDEX "lift"."_CategoryToPost_AB_unique" ON "_CategoryToPost"("A","B")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191016111406-first
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,30 @@
+generator photon {
+  provider = "photonjs"
+}
+
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+}
+
+model User {
+  id    String  @default(cuid()) @id
+  email String  @unique
+  name  String?
+  posts Post[]
+}
+
+model Post {
+  id         String     @default(cuid()) @id
+  published  Boolean    @default(true)
+  title      String
+  content    String?
+  author     User?
+  categories Category[]
+}
+
+model Category {
+  id    String @id @default(cuid())
+  name  String
+  posts Post[]
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191016111406-first)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191016111406-first'

const photon = new Photon()

async function main() {
    const result = await photon.users()
    console.dir(result, { depth: null })
}

main()
```

# Migration `20191016160009-rename-comment-item-column`

This migration has been generated by Vincent Poulain at 10/16/2019, 4:00:09 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Item" DROP COLUMN "note",
ADD COLUMN "comment" text   ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191016155854-tottem..20191016160009-rename-comment-item-column
--- datamodel.dml
+++ datamodel.dml
@@ -51,9 +51,9 @@
   author     String
   type       ItemType
   productUrl String
   imageUrl   String?
-  note       String?
+  comment       String?
   collection Collection
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191016160009-rename-comment-item-column)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191016160009-rename-comment-item-column'

const photon = new Photon()

async function main() {
    const result = await photon.users()
    console.dir(result, { depth: null })
}

main()
```

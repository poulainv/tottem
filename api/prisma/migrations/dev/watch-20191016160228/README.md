# Migration `watch-20191016160228`

This migration has been generated by Vincent Poulain at 10/16/2019, 4:02:35 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..watch-20191016160228
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,68 @@
+generator photon {
+  provider = "photonjs"
+}
+
+datasource db {
+  provider = env("DATABASE_PROVIDER")
+  url      = env("DATABASE_URL")
+}
+
+model User {
+  id        String    @default(cuid()) @id
+  slug      String    @unique
+  firstname String
+  biography String
+  social    Social?
+  label     String?
+  sections  Section[]
+
+}
+
+model Social {
+  id       String  @default(cuid()) @id
+  linkedin String?
+  github   String?
+  mail     String?
+  youtube  String?
+  website  String?
+}
+
+model Section {
+  id          String       @default(cuid()) @id
+  name        String
+  index       Int          @default(0)
+  collections Collection[]
+  author      User
+}
+
+model Collection {
+  id      String  @id @default(cuid())
+  name    String
+  detail  String?
+  items   Item[]
+  date    String?
+  author  User
+  section Section
+}
+
+model Item {
+  id         String     @default(cuid()) @id
+  title      String
+  author     String
+  type       ItemType
+  productUrl String
+  imageUrl   String?
+  comment       String?
+  collection Collection?
+}
+
+
+enum ItemType {
+  Book
+  Album
+  Movie
+  Video
+  People
+  Paper
+  Podcast
+}
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191016160228)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191016160228'

const photon = new Photon()

async function main() {
    const result = await photon.users()
    console.dir(result, { depth: null })
}

main()
```
